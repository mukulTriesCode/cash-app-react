import React from "react";

const LoadingSpinner: React.FC = () => (
  <div className="flex h-[calc(100vh-81px)] xs:h-[calc(100vh-95px)] w-full justify-center items-center">
    <div className="ping">
    </div>
  </div>
);

export default LoadingSpinner;
